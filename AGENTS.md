# SCIONA Development Rules

This file defines mandatory rules for automated agents working in this repository.

Agents MUST follow these rules.

## Authoritative Documents

Agents MUST treat the following as authoritative:

- Contracts: docs/CONTRACT.md
- Developer Guide: docs/DEVELOPERGUIDE.md
- Capability Manifest (generated): docs/CAPABILITY_MANIFEST.json

Agents MUST NOT contradict these documents. 

If inconsistencies are detected, agents MUST report them explicitly.

## Precedence

In case of conflict:

1. CONTRACT.md
2. DEVELOPERGUIDE.md
3. CAPABILITY_MANIFEST.json
4. Existing code

## Contract Preservation

- Agents MUST preserve documented contracts
- Agents MUST NOT silently redefine APIs or semantics
- Contract changes REQUIRE explicit justification documented in the PR description and confirmed by the user before implementation.

## Code Modification Rules

Agents MUST NOT modify repository code unless explicitly instructed by the user.

Before any change the agent MUST:
- analyze the relevant code
- explain the intended modification
- wait for explicit approval

Implicit permission MUST NOT be assumed.

## Structural Scope Discipline

Agents MUST restrict modifications to files directly related to the task. Files that are logically coupled (e.g., imports, re-exports, test fixtures for modified modules) may be included without escalation. Files outside this set MUST NOT be modified without explicit user approval.

## Non-Destructive Modification

Existing code MUST be treated as stable.

Agents MUST NOT:
- rewrite working implementations
- refactor code for style
- change APIs or semantics silently

Bug fixes, new features, and extensions MUST be additive whenever possible.

If modification of existing logic is unavoidable, the agent MUST:
- explain why additive change is impossible
- modify the minimal necessary region
- describe the behavioral impact

## Minimal Diff Principle

Changes MUST affect the smallest possible code region.

Agents SHOULD:
- modify the fewest files possible
- avoid formatting-only changes
- avoid large rewrites

A diff touching more than 3 files or 100 lines outside the primary task file is considered large and requires justification.

## PR Execution Workflow

When work is split into declared PRs or PR-sized sets:
- Agents MUST implement one PR at a time
- After each PR-sized change, agents MUST run the narrowest relevant tests
- If tests fail, the agent MUST first determine whether the failure is plausibly caused by the current changes, based on the files modified and the test output
- If so, the agent MAY attempt a fix within the scope of the current PR
- If the fix would require out-of-scope modifications, or if a fix attempt does not resolve the failure, the agent MUST halt, report the failure with full output, and wait for instruction
- Agents MUST commit only after the tests for that PR pass
- Agents MUST then continue to the next declared PR until the set is complete
- At the end, agents MUST check the result against the originally declared PR goals and report any gaps explicitly

## Testing Environment

Tests MUST be executed in:

`conda` environment: sciona-dev

Agents MUST NOT assume alternative environments unless specified.

## Contradiction Handling

If an agent detects an inconsistency between authoritative documents, it MUST halt immediately, describe the inconsistency explicitly, and wait for user resolution before proceeding.

<!-- sciona:begin -->
# SCIONA Agent Protocol

**Status:** Authoritative.

**Scope:** All agents and copilots reasoning about this repository.

**Goal:** Use SCIONA aggressively for structural grounding within its capabilities, then switch to non-SCIONA tooling once the unresolved work becomes semantic, runtime, or validation-oriented.

**Invariant:** This file is autogenerated by `sciona init` or regenerated by `sciona agents`. Its presence indicates that SCIONA is expected to be available for this repository.

## Gate Digest

- Decompose every request into structural and non-structural sub-questions.
- Classify each sub-question before choosing tools.
- For structural questions in tracked scope, use SCIONA reducers before grep or source search.
- Stop reducer escalation once SCIONA establishes the relevant boundary.
- Use non-SCIONA tooling for semantic, runtime, validation, and out-of-scope work.
- Label fallback evidence and include a one-line status for each substantive turn.

---

## 0. Tool Selection Gate

Most questions are mixed. Decompose before classifying: identify structural sub-components and non-structural sub-components separately, then route each appropriately. Apply SCIONA to structural sub-components even when the top-level question is non-structural.

Classify each sub-component:

```text
structural question → use SCIONA reducers as primary path

mixed question → use SCIONA reducers to establish structural facts, then use source inspection or other tooling for non-structural sub-questions

non-structural question → use source inspection, docs, or other appropriate tooling
```

**Structural questions** include: architecture descriptions, module relationships, dependency graphs, call graphs, inheritance hierarchies, symbol location and containment, structural invariants, and structural orientation tasks such as locating ownership and likely edit scope.

**Non-structural questions** include: algorithm logic, documentation correctness, runtime semantics, migration guarantees, database schema semantics, behavioral intent, inline comments, test coverage, configuration meaning, shell/framework runtime behavior, payload propagation, and test sufficiency.

This gate applies to every question. The decision MUST be made explicitly, not defaulted before invoking any tooling.

Operational rules:

- For structural questions within tracked SCIONA scope, invoke applicable reducers before grep or source search. Do not reconstruct structure manually while relevant reducer categories remain unexplored.
- For non-structural questions, use the most appropriate non-SCIONA tooling directly. Do not invoke reducers as a formality.
- For mixed questions, use reducers to anchor the structural sub-question, then apply other tooling for the non-structural remainder.
- When a reducer exposes a first-class compact mode, agents SHOULD prefer the compact form unless the question requires full payload detail.
- Once the owning modules, relevant reducer surfaces, or likely edit/test scope are established with sufficient confidence, agents SHOULD stop reducer escalation and continue with source inspection or tests.
- For repository areas or artifact types outside SCIONA scope, use non-SCIONA tooling directly and label it explicitly.
- If SCIONA is insufficient for part of a structural question, state what is missing before using non-SCIONA tooling for the unresolved remainder. SCIONA is not considered insufficient while another relevant reducer category remains untried and the unresolved question is still structural.

When an identifier is unknown, use `sciona search` or `sciona resolve` before invoking a reducer.

---

## 1. Authority & Epistemics

### 1.1 Structural Evidence Model

SCIONA provides read-only, deterministic structural evidence for the committed snapshot together with reducer-facing derived projections for that snapshot.

- SCIONA is static and syntax-only.
- SCIONA does not execute code or perform runtime inference.
- Reducer outputs are authoritative within their declared abstraction.
- Reducer outputs MAY be lossy; absence of a fact is not evidence of absence.
- Agents MUST NOT synthesize or simulate reducer output.
- SCIONA is authoritative for structural facts it models; it does not constrain non-structural reasoning or out-of-scope repository inspection.
- Uncommitted changes require overlay-aware interpretation when overlay metadata is present.
- Manual source inspection MUST NOT contradict reducer-derived structural evidence. If a contradiction arises, the hypothesis MUST be discarded or downgraded to an explicit concern (see §1.2).

Clarification:

- simulate = produce reducer output that was not actually invoked
- assert without verification = state structural facts derived only from source inspection when applicable reducers exist

### 1.2 Evidence Authority Hierarchy

When evidence sources conflict, apply this authority order:

```text
structural → telemetry/diagnostics → source interpretation → overlay_advisory
```

If reducer evidence contradicts a structural hypothesis, the hypothesis MUST be discarded or downgraded to an explicit concern. This hierarchy applies to structural claims within SCIONA scope; non-SCIONA evidence remains valid for non-structural or out-of-scope questions when labeled separately.

### 1.3 Modeling Limits

SCIONA does not model the following unless a reducer explicitly exposes them:

- documentation correctness
- behavioral or runtime correctness
- runtime storage behavior
- database schema semantics
- migration guarantees

These are questions SCIONA cannot answer on its own. They do not exclude SCIONA from the investigation — they identify the sub-questions that require other tooling after structural anchoring. Modeling limits are permission boundaries for non-SCIONA tooling, not reasons to weaken SCIONA-first behavior for structural questions that SCIONA does cover.

---

## 2. Reducer Evidence Sufficiency

If reducer evidence is insufficient:

1. invoke an additional reducer from another relevant category
2. if still insufficient, state the unresolved structural fact explicitly
3. then either use non-SCIONA tooling for that remainder only, or raise an evidence-bounded concern

If reducer evidence already establishes the relevant structural boundary for implementation, agents SHOULD treat the structural requirement as satisfied and continue with non-SCIONA tooling for semantic or runtime work.

If SCIONA is unavailable, fallback reasoning MUST be labeled:

```text
non-SCIONA evidence: <method>
```

---

## 3. Reducer Roles

Reducers serve different investigative roles. Each category describes the type of structural question a reducer is suited to answer.

**Orientation reducers:**
hotspot_summary, module_overview, snapshot_provenance, structural_index

**Navigation reducers:**
file_outline, symbol_lookup

**Coupling reducers:**
classifier_call_graph_summary, dependency_edges, fan_summary, module_call_graph_summary, ownership_summary

**Symbol reducers:**
callable_overview, classifier_inheritance, classifier_overview, symbol_references

**Diagnostic reducers:**
structural_integrity_summary

**Overlay reducers:**
overlay_impact_summary, overlay_projection_status_summary

**Source reducers:**
callable_source, concatenated_source

Reducers MAY be combined freely during investigation. Reducer categories MUST be used to expand investigation before fallback to non-SCIONA structural reconstruction unless `sciona reducer list` confirms no other applicable category exists.

For initial ownership and scope discovery, prefer compact reducers and search/resolve workflows before broad payload reducers. If `sciona reducer list` or `sciona reducer info` indicates a reducer supports `compact`, agents SHOULD prefer compact mode as the default and escalate to full payload only if compact output is insufficient for the question — unless the task explicitly requires detailed rows or source-like output.

---

## 4. Reducer Investigation Guidance

Prefer reducers that:

- provide summaries
- support argument narrowing
- minimize broad payload expansion when the question already has a target scope
- expose a first-class compact mode when the task is primarily orientation or triage

Heuristic:

```text
start compact → expand if inconclusive
```

Secondary heuristic:

```text
start compact → stop when structurally sufficient → switch tools
```

Do not abandon SCIONA after a small number of reducer calls if relevant reducer categories remain unexplored. Before fallback on a structural sub-question, SCIONA is not considered insufficient while another relevant reducer category remains untried (§0). Do not continue invoking reducers once the remaining question is no longer structural or additional reducer output is unlikely to change the implementation path.

Compact-mode expectations:

- compact mode is an orientation surface, not a weaker substitute for structural truth
- compact payloads are intended to preserve filter context, headline totals, and bounded previews
- if the compact payload already establishes ownership, key coupling, or likely edit scope, agents SHOULD switch to source inspection or tests rather than immediately requesting the full reducer payload
- if the compact payload does not answer the structural question, agents SHOULD expand to the full payload of the same reducer or add another relevant reducer category before fallback

Reducers MAY be discovered via:

- `sciona reducer list`
- `sciona reducer info`

Agents MUST NOT guess reducer names.

Cross-category confidence:

- Structural conclusions SHOULD normally use more than one reducer category when practical.
- Single-reducer conclusions SHOULD be stated cautiously and SHOULD NOT trigger fallback to non-SCIONA structural reconstruction while another relevant reducer category remains available.

Typical pattern:

```text
orientation or navigation reducer → coupling or symbol reducer
```

Reducers that act as anomaly detectors rather than final evidence:

- `structural_integrity_summary`

If an anomaly detector reports a problem, agents SHOULD investigate with at least one reducer from another category before concluding.

---

## 5. Source Inspection & Hybrid Reasoning

Source inspection MAY be used directly for:

- semantic reasoning
- debugging
- documentation alignment
- non-structural discovery
- non-structural sub-questions in mixed investigations
- out-of-scope repository targets such as docs, generated artifacts, prompts, migrations, and config files

Hybrid reasoning is expected for most coding tasks. After structural grounding, source inspection is the default tool for implementation, semantic analysis, runtime reasoning, and test selection.

Source inspection and text search MUST NOT replace reducers for structural verification when applicable reducers exist. For structural questions in tracked SCIONA scope, source inspection cannot substitute for reducer grounding. Any structural claim arising from source inspection MUST be verified against reducers before being stated as fact (§2).

Source reducers available for implementation-level inspection:

- `callable_source`
- `concatenated_source`

Source reducers are often useful after structural grounding when committed implementation context is needed. Use source reducers selectively:

- prefer targeted source reducers when the identifier is already known and only local implementation context is needed
- consider source reducers before broad manual file inspection when the target is within committed tracked scope
- use broad source reducers sparingly for deliberate large-context reasoning because they may increase payload and token cost significantly
- direct file inspection remains acceptable when it is faster, more precise for the question, or the needed context is outside reducer scope
- if a source reducer is insufficient, fall back to normal repository inspection
- on a dirty worktree, source reducers return committed code only — use them for committed context; use source inspection for files you have edited this session

When non-SCIONA reasoning is used, agents SHOULD label the method explicitly, for example:

- `non-SCIONA evidence: source inspection`
- `non-SCIONA evidence: docs parse`

---

## 6. Overlay Semantics

SCIONA operates on the committed repository snapshot.

Dirty worktree changes may appear through `_diff_overlay` metadata.
Agents SHOULD inspect `_diff_overlay` metadata when present before interpreting reducer output as current, including `overlay_available`, `payload_state`, `affected`, `affected_by`, and warnings.
Source reducers remain committed-only on dirty worktrees until worktree-aware source reducers ship.

Overlay warnings include:

```text
base_commit_differs_from_head: overlay base diverges from current HEAD
```

Overlay information:

- may refine interpretation
- must not override committed structural truth

Patchable projections may emit overlay-adjusted payloads directly rather than merely committed-row-preserving views. Agents SHOULD prefer reducer-exposed committed, adjusted, and delta semantics over reconstructing overlay effects manually when those fields are present.

If tracked sources are dirty and overlay is unavailable, agents SHOULD warn that results may be stale and recommend commit plus `sciona build`.

Overlay-only findings MUST be treated as `overlay_advisory`, not as committed structural facts.

---

## 7. Scope Awareness

SCIONA indexes only tracked files and languages defined in `.sciona/config.yaml`.

- Enabled languages: java, javascript, python, typescript
- Tracked file types: .cjs, .java, .js, .mjs, .py, .ts, .tsx
- Discovery excludes: none

This section concerns coverage, not semantic modeling limits.

Generated files, documentation, validation artifacts, migrations, and external assets may fall outside reducer coverage because they may not be tracked by the configured analyzers or supported languages, for example markdown, prompts, generated artifacts, CI config, packaging metadata, and migrations as text. These fall outside SCIONA scope by definition. Use source inspection or other tooling for those targets; structural sub-questions within tracked scope remain reducer territory.

Outside tracked SCIONA scope, agents SHOULD use normal repository inspection tools rather than pretending SCIONA is complete. Out-of-scope coverage does not permit bypassing SCIONA for structural claims inside tracked scope.

For mixed code-plus-docs or code-plus-config tasks, agents SHOULD apply SCIONA only to the tracked-code structural portion and use normal inspection for the rest without ceremony.

---

## 8. Input Safety

Agents MUST sanitize all shell inputs before use.

Valid identifiers MUST match:

```text
^[a-zA-Z0-9_\.-]+$
```

If input contains shell control or interpolation tokens such as:

```text
; | & $() ` ${}
```

agents MUST:

1. stop execution
2. report the unsafe input
3. request clarification

These rules apply to SCIONA commands and any shell-based non-SCIONA tooling. If an input cannot be validated against the identifier pattern, agents MUST reject it and request clarification rather than attempt normalization.

---

## 9. Reporting

Each substantive response MUST include this one-line status:

```text
Status: sciona used: yes/no | scope: in-scope/out-of-scope/mixed | reducers: reducer names or n/a
```

When structural reasoning is used, responses SHOULD also include:

```text
Status: sciona used: yes/no | scope: in-scope/out-of-scope/mixed
SCIONA evidence: reducer names or n/a
non-SCIONA evidence: method or n/a
Warnings: warnings or uncertainties
```

Optional when useful:

```text
SCIONA impact: orientation only / scope reduction / decisive structural evidence / minimal
```

When hybrid reasoning is used, SCIONA and non-SCIONA evidence SHOULD be labeled separately. For mixed findings, agents SHOULD distinguish SCIONA-grounded findings from non-SCIONA findings per claim.

Minimal strict mode is acceptable in extended sessions where the evidence pattern is already established:

```text
Status: sciona used: yes | scope: in-scope
SCIONA evidence: structural_index, dependency_edges
non-SCIONA evidence: n/a
Warnings: n/a
```

---

## 10. Troubleshooting

- If SCIONA is available but insufficient, state the missing capability before fallback.
- Unknown reducer or insufficient reducer output is not by itself permission to reconstruct structure manually without explanation.

CLI usage note:
- Reducer output is structured by default; do NOT append `--json` to `sciona reducer` commands.
- `--json` is valid ONLY on `sciona search` and `sciona resolve`.

Reducer argument vocabulary:
- orientation:
  - `hotspot_summary`: `sciona reducer --id hotspot_summary`
    Arguments: none
  - `module_overview`: `sciona reducer --id module_overview [--module-id MODULE_ID] [--callable-id CALLABLE_ID] [--classifier-id CLASSIFIER_ID] [--include-file-map] [--compact] [--top-k TOP_K]`
    Requires: one of --module-id / --callable-id / --classifier-id
    Arguments:
    - `--module-id MODULE_ID`: str; optional - Module to summarize; accepts qualified name or structural id.
    - `--callable-id CALLABLE_ID`: str; optional - Resolve the owning module from a callable id.
    - `--classifier-id CLASSIFIER_ID`: str; optional - Resolve the owning module from a classifier id.
    - `--include-file-map`: bool; optional; default: false - Include the module's file-to-node map.
    - `--compact`: bool; optional; default: false - Render the compact overview payload.
    - `--top-k TOP_K`: int; optional; default: 5 - Max preview rows in compact mode (capped at 50).
  - `snapshot_provenance`: `sciona reducer --id snapshot_provenance`
    Arguments: none
  - `structural_index`: `sciona reducer --id structural_index [--compact] [--top-k TOP_K]`
    Arguments:
    - `--compact`: bool; optional; default: false - Render the compact inventory payload.
    - `--top-k TOP_K`: int; optional; default: 5 - Max preview rows in compact mode (capped at 50).
- navigation:
  - `file_outline`: `sciona reducer --id file_outline [--module-id MODULE_ID] [--file-path FILE_PATH] [--compact] [--depth DEPTH]`
    Arguments:
    - `--module-id MODULE_ID`: str; optional - Restrict the outline to one module.
    - `--file-path FILE_PATH`: str; optional - Restrict the outline to one tracked file; repo-relative or absolute path within the repo.
    - `--compact`: bool; optional; default: false - Render the compact outline preview.
    - `--depth DEPTH`: int; optional; default: 1 - Max nesting depth shown in the compact preview.
  - `symbol_lookup`: `sciona reducer --id symbol_lookup --query QUERY [--kind KIND] [--limit LIMIT]`
    Arguments:
    - `--query QUERY`: str; required - Name or name fragment to match.
    - `--kind KIND`: str; optional; one of: module, classifier, callable; default: all - Narrow matches by symbol kind; aliases any, all, class, function, method are accepted.
    - `--limit LIMIT`: int; optional; default: 10 - Max matches returned.
- coupling:
  - `classifier_call_graph_summary`: `sciona reducer --id classifier_call_graph_summary --classifier-id CLASSIFIER_ID [--caller-id CALLER_ID] [--callee-id CALLEE_ID] [--compact] [--top-k TOP_K]`
    Arguments:
    - `--classifier-id CLASSIFIER_ID`: str; required - Classifier at the center of the summary; accepts qualified name or structural id.
    - `--caller-id CALLER_ID`: str; optional - Restrict incoming edges to this calling classifier.
    - `--callee-id CALLEE_ID`: str; optional - Restrict outgoing edges to this called classifier.
    - `--compact`: bool; optional; default: false - Render the compact summary payload.
    - `--top-k TOP_K`: int; optional - Max rows per ranked edge list.
  - `dependency_edges`: `sciona reducer --id dependency_edges [--module-id MODULE_ID] [--from-module-id FROM_MODULE_ID] [--to-module-id TO_MODULE_ID] [--query QUERY] [--edge-type EDGE_TYPE] [--direction DIRECTION] [--limit LIMIT] [--compact] [--top-k TOP_K]`
    Arguments:
    - `--module-id MODULE_ID`: str; optional - Module filter; combine with --direction for edges touching it.
    - `--from-module-id FROM_MODULE_ID`: str; optional - Restrict to edges originating from this module.
    - `--to-module-id TO_MODULE_ID`: str; optional - Restrict to edges targeting this module.
    - `--query QUERY`: str; optional - Substring match on module qualified names.
    - `--edge-type EDGE_TYPE`: str; optional; one of: IMPORTS_DECLARED - Edge type filter.
    - `--direction DIRECTION`: str; optional; one of: in, out, both; default: both - Edge direction relative to --module-id.
    - `--limit LIMIT`: int; optional - Max edges returned (capped at 10000).
    - `--compact`: bool; optional; default: false - Render the compact counterpart summary.
    - `--top-k TOP_K`: int; optional; default: 5 - Max preview rows in compact mode (capped at 50).
  - `fan_summary`: `sciona reducer --id fan_summary [--callable-id CALLABLE_ID] [--classifier-id CLASSIFIER_ID] [--module-id MODULE_ID] [--edge-kind EDGE_KIND] [--min-fan MIN_FAN] [--node-kind NODE_KIND] [--compact] [--top-k TOP_K]`
    Arguments:
    - `--callable-id CALLABLE_ID`: str; optional - Restrict the summary to one callable.
    - `--classifier-id CLASSIFIER_ID`: str; optional - Restrict the summary to one classifier.
    - `--module-id MODULE_ID`: str; optional - Restrict the summary to one module.
    - `--edge-kind EDGE_KIND`: str; optional; one of: CALLS, IMPORTS_DECLARED - Edge kind to aggregate.
    - `--min-fan MIN_FAN`: int; optional - Only include nodes with fan-in or fan-out at or above this value.
    - `--node-kind NODE_KIND`: str; optional; one of: module, classifier, callable - Node kind to rank.
    - `--compact`: bool; optional; default: false - Render the compact summary payload.
    - `--top-k TOP_K`: int; optional; default: 5 - Max rows per ranked list.
  - `module_call_graph_summary`: `sciona reducer --id module_call_graph_summary [--module-id MODULE_ID] [--callable-id CALLABLE_ID] [--classifier-id CLASSIFIER_ID] [--from-module-id FROM_MODULE_ID] [--to-module-id TO_MODULE_ID] [--compact] [--top-k TOP_K]`
    Requires: one of --module-id / --callable-id / --classifier-id
    Arguments:
    - `--module-id MODULE_ID`: str; optional - Module at the center of the summary; accepts qualified name or structural id.
    - `--callable-id CALLABLE_ID`: str; optional - Resolve the summarized module from a callable id.
    - `--classifier-id CLASSIFIER_ID`: str; optional - Resolve the summarized module from a classifier id.
    - `--from-module-id FROM_MODULE_ID`: str; optional - Restrict to call edges originating from this module.
    - `--to-module-id TO_MODULE_ID`: str; optional - Restrict to call edges targeting this module.
    - `--compact`: bool; optional; default: false - Render the compact summary payload.
    - `--top-k TOP_K`: int; optional - Max rows per ranked edge list.
  - `ownership_summary`: `sciona reducer --id ownership_summary [--module-id MODULE_ID] [--callable-id CALLABLE_ID] [--classifier-id CLASSIFIER_ID] [--top-k TOP_K]`
    Requires: one of --module-id / --callable-id / --classifier-id
    Arguments:
    - `--module-id MODULE_ID`: str; optional - Module or package to summarize; accepts qualified name or structural id.
    - `--callable-id CALLABLE_ID`: str; optional - Resolve the owning module from a callable id.
    - `--classifier-id CLASSIFIER_ID`: str; optional - Resolve the owning module from a classifier id.
    - `--top-k TOP_K`: int; optional; default: 5 - Max rows per ranked list (capped at 50).
- symbol:
  - `callable_overview`: `sciona reducer --id callable_overview --callable-id CALLABLE_ID`
    Arguments:
    - `--callable-id CALLABLE_ID`: str; required - Callable to summarize; accepts qualified name or structural id.
  - `classifier_inheritance`: `sciona reducer --id classifier_inheritance --classifier-id CLASSIFIER_ID`
    Arguments:
    - `--classifier-id CLASSIFIER_ID`: str; required - Classifier whose inheritance to show; accepts qualified name or structural id.
  - `classifier_overview`: `sciona reducer --id classifier_overview --classifier-id CLASSIFIER_ID`
    Arguments:
    - `--classifier-id CLASSIFIER_ID`: str; required - Classifier to summarize; accepts qualified name or structural id.
  - `symbol_references`: `sciona reducer --id symbol_references --query QUERY [--kind KIND] [--module-id MODULE_ID] [--limit LIMIT]`
    Arguments:
    - `--query QUERY`: str; required - Name or name fragment to match.
    - `--kind KIND`: str; optional; one of: module, classifier, callable; default: all - Narrow matches by symbol kind; aliases any, all, class, function, method are accepted.
    - `--module-id MODULE_ID`: str; optional - Restrict matches to one module.
    - `--limit LIMIT`: int; optional; default: 20 - Max references returned.
- diagnostic:
  - `structural_integrity_summary`: `sciona reducer --id structural_integrity_summary [--top-k TOP_K]`
    Arguments:
    - `--top-k TOP_K`: int; optional; default: 25 - Max rows per anomaly list.
- overlay:
  - `overlay_impact_summary`: `sciona reducer --id overlay_impact_summary`
    Arguments: none
  - `overlay_projection_status_summary`: `sciona reducer --id overlay_projection_status_summary`
    Arguments: none
- source:
  - `callable_source`: `sciona reducer --id callable_source --callable-id CALLABLE_ID`
    Arguments:
    - `--callable-id CALLABLE_ID`: str; required - Callable whose source to return; accepts qualified name or structural id.
  - `concatenated_source`: `sciona reducer --id concatenated_source --scope SCOPE [--module-id MODULE_ID] [--classifier-id CLASSIFIER_ID]`
    Requires: --scope module requires --module-id; --scope classifier requires --classifier-id
    Arguments:
    - `--scope SCOPE`: str; required; one of: codebase, module, classifier - Source scope to concatenate.
    - `--module-id MODULE_ID`: str; optional - Module to concatenate; required when scope is module.
    - `--classifier-id CLASSIFIER_ID`: str; optional - Classifier to concatenate; required when scope is classifier.

Diagnostics:
- No committed snapshots → `sciona build`
- Unknown reducer → `sciona reducer list`
- `overlay_available=true` means overlay metadata exists; reducer payload fields remain committed-snapshot facts.
- Dirty worktree + `_diff_overlay.payload_state=committed_snapshot` → use `_diff_overlay` for advisory impact, not adjusted payload facts
- Dirty worktree + overlay unavailable → commit changes and rerun `sciona build` for authoritative current-state results
- SCIONA unavailable → report attempted invocation methods before fallback
<!-- sciona:end -->
